## 1. 后端认证基础

- [ ] 1.1 新增管理员账号模型，包含用户名、密码哈希参数、创建时间和更新时间
- [ ] 1.2 新增管理员账号 JSON 仓库，读写 `data/admin/admin.json` 并区分不存在与损坏状态
- [ ] 1.3 新增 PBKDF2 密码哈希与验证工具，保存随机 salt、hash、算法和迭代次数
- [ ] 1.4 新增认证请求与响应 DTO，覆盖状态、初始化、登录和当前用户信息

## 2. 后端认证服务与接口

- [ ] 2.1 新增认证服务，支持初始化状态查询、单管理员初始化、登录校验和登出
- [ ] 2.2 实现初始化规则：用户名非空、密码至少 8 位、重复初始化返回冲突、初始化后不自动登录
- [ ] 2.3 实现登录规则：未初始化拒绝登录、错误用户名或密码统一返回未认证、成功后写入 Session
- [ ] 2.4 新增 `/api/auth/status`、`/api/auth/setup`、`/api/auth/login`、`/api/auth/logout` 控制器接口
- [ ] 2.5 更新全局异常响应或业务异常使用方式，确保 400、401、409 语义清晰

## 3. 后端管理 API 门禁

- [ ] 3.1 新增认证拦截器，拦截除 `/api/auth/**` 之外的 `/api/**` 管理接口
- [ ] 3.2 在 Web MVC 配置中注册认证拦截器并确保静态资源不受影响
- [ ] 3.3 确认未初始化状态下除认证接口外的管理 API 仍不可访问

## 4. 前端认证能力

- [ ] 4.1 新增认证 API 封装，覆盖状态查询、初始化、登录和登出
- [ ] 4.2 新增 `/auth` 路由和认证页面，根据状态显示初始化表单或登录表单
- [ ] 4.3 实现初始化表单校验：用户名必填、密码至少 8 位、确认密码匹配
- [ ] 4.4 实现初始化成功后提示用户并切换到登录模式，不设置前端已登录状态
- [ ] 4.5 实现登录成功后跳转到原目标页面或默认订阅源管理页面，登录失败停留表单并展示错误

## 5. 前端路由与后台布局

- [ ] 5.1 新增路由守卫或根组件认证检查，未初始化或未登录时导航到 `/auth`
- [ ] 5.2 更新 Axios 响应拦截器，非认证管理 API 返回 401 时跳转认证页，认证接口错误留在表单内处理
- [ ] 5.3 更新后台 header，展示当前管理员用户名和退出登录按钮
- [ ] 5.4 实现退出登录交互，调用登出接口后回到认证页面

## 6. 自动化测试

- [ ] 6.1 添加后端测试覆盖未初始化状态查询、初始化成功、短密码失败、重复初始化失败
- [ ] 6.2 添加后端测试覆盖登录成功、登录失败、未初始化登录失败、登出后状态失效
- [ ] 6.3 添加后端测试覆盖未登录访问受保护 API 返回 401，登录后访问同一 API 成功
- [ ] 6.4 添加后端测试覆盖管理员文件损坏时不允许重新初始化

## 7. 构建与手动验证

- [ ] 7.1 运行 Maven 测试，确认后端行为通过
- [ ] 7.2 运行前端构建，确认 TypeScript 与 Vite 构建通过
- [ ] 7.3 启动应用并手动验证首次初始化、初始化后登录、刷新保持会话、登出后拦截 API
- [ ] 7.4 检查 `.superpowers/` 是否需要加入 `.gitignore`，避免提交 brainstorming 临时可视化产物
