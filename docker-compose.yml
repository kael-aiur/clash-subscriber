services:
  clash-subscriber:
    build: .
    container_name: clash-subscriber
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Asia/Shanghai
      - SERVER_PORT=${SERVER_PORT:-8080}
    restart: unless-stopped
