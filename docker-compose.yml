services:
  clash-subscriber:
    build: .
    container_name: clash-subscriber
    ports:
      - "31192:31192"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Asia/Shanghai
      - SERVER_PORT=${SERVER_PORT:-31192}
    restart: unless-stopped
