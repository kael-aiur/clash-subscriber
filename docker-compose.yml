version: '3.8'

services:
  clash-subscriber:
    build: .
    container_name: clash-subscriber
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
